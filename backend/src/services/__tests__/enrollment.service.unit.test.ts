import { describe, it } from 'vitest';

describe('enrollment.service unit', () => {
    it.todo('enrollInCourse creates enrollment for valid user and course');
    it.todo('enrollInCourse rejects duplicate enrollment');
    it.todo('getUserEnrollments returns populated user enrollments');
    it.todo('checkUserEnrollment returns true when enrolled');
});
