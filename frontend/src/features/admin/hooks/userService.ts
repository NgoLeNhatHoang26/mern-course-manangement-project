export interface IUser {
    _id: string
    userName: string
    email: string
    role: string
    isActive: boolean
    enrollmentCount: number
    createdAt: string
}

export const UserService = {

}